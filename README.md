# cardinals-milb-analysis

Access instructions:
git clone <your-repo>
cd cardinals-player-development

python3 -m venv .venv
source .venv/bin/activate

pip install -r requirements.txt

streamlit run src/app.py